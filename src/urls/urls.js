const baseURL = 'https://jsonplaceholder.typicode.com'

const users = '/users'
const comments = '/comments'

const urls = {
    url_users: users,
    url_comments: comments
}

export {
    urls,
    baseURL
}